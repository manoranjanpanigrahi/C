#include<stdio.h>
void main()
{
   int length,breadth,area;
   printf("the leangth is : " );
   scanf("%d", &length);

   printf("the breadth is : ");
   scanf("%d", &breadth);

   area= length*breadth;
   printf("the area is: %d", area);
   
}